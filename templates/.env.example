# Google Search Console + Claude Code Integration
# Environment Variables Template
#
# Instructions:
# 1. Copy this file to .env in your project root
# 2. Fill in your values
# 3. Never commit .env to version control
# 4. Add .env to .gitignore

# GSC Credentials
# Path to your service account JSON key file
GSC_CREDENTIALS_PATH=~/.gsc-credentials/service-account-key.json

# GSC Property Information
# The domain/property in Google Search Console
GSC_SITE_URL=https://example.com

# Optional: If using property ID instead of URL
# Format: sc-domain:example.com
GSC_PROPERTY_ID=

# MCP Server Configuration
# Port for MCP server (if applicable)
MCP_SERVER_PORT=3000

# Python Environment
# Path to Python interpreter (auto-detected if not set)
PYTHON_PATH=

# Virtual Environment
# Path to virtual environment
VENV_PATH=.venv

# Logging
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Optional: API Rate Limiting
# Max queries per minute
API_MAX_QUERIES_PER_MINUTE=100

# Optional: Data Caching
# Cache query results for X seconds
CACHE_TTL=3600

# Optional: Multiple Properties
# If managing multiple GSC properties
# SECONDARY_SITE_URL=https://other-domain.com
# SECONDARY_CREDENTIALS_PATH=~/.gsc-credentials/secondary-key.json
